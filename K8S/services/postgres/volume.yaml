apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pc-volume-pvc
  labels:
    app: postgres
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
